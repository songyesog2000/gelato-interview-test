# 笔试与评测流程说明

本文档基于当前仓库的实际设计（三层结构的"轨迹标注工具"），说明从编码到提交的完整操作流程。
仅作为候选人自己的操作手册，不替代笔试原始通知的任何要求；如有出入以原始通知为准。

## 1. 整体流程概览

```
阶段一：初始实现  ──┐
阶段二：功能扩展  ──┼── 同一项目 / 同一工作区 / 同一会话 / 同一语言
阶段三：缺陷处理或代码理解 ──┘
        │
        ▼
  正常退出 CLI，导出本次会话原始 .jsonl
        │
        ▼
  人工通读轨迹（不借助 AI），用本仓库的标注工具逐条填写
        │
        ▼
  导出 CSV → 誊入"笔试作答"表 + 上传原始 .jsonl + 填写最终代码链接
```

三个阶段共用一个 CLI 会话，因此本仓库当前的 `server/`、`public/`、`data/` 三部分代码本身就是
阶段一的交付物；阶段二、三在此基础上继续迭代，不新开项目或新开会话。

## 2. 阶段一：初始实现（已完成）

当前状态：已从空目录生成完整项目，包含后端（`server/`）、前端（`public/`）、面试者数据目录
（`data/`）。具体功能、输入输出、限制见根目录 `README.md`。这一阶段的验收关注点是：项目能否
安装运行、是否产生可观察结果、结构是否支持后续迭代。

## 3. 阶段二：功能扩展

候选人基于阶段一的**实际代码**（不是原始需求）提出 3-5 条可分别验收的新需求，需覆盖校验、错误
处理、兼容、测试或权限中至少一类，且互不矛盾、在现有模块上增量修改。提出需求前建议先看一遍当前
代码结构（`server/lib/`、`server/routes/api.js`、`public/app.js`），确保提出的需求是针对真实
存在的模块，而不是泛泛而谈。

## 4. 阶段三：缺陷处理 / 代码理解

- 若在使用阶段二产物过程中发现可复现缺陷：提供复现步骤、实际现象、期望行为，要求定位、修复并
  解释原因；修复前后需执行同一复现检查并保留实际输出。
- 若没有可复现缺陷：选择当前项目一条关键流程（例如"从选中会话文件到页面渲染出记录列表"或
  "保存标注到导出 CSV"），要求说明涉及模块及调用关系、关键数据/状态变化，以及一个边界或异常
  场景的处理方式。不虚构、不人为植入缺陷。

## 5. 三阶段结束后：导出原始会话

1. 正常退出 CLI（不要用会中断落盘的参数）。
2. macOS 下：Finder 按 `Command+Shift+G`，粘贴 `~/.claude/projects/`，找到本项目对应的子目录
   （目录名由项目路径转换而来）。
3. 按作答时间找到本次会话的 `<session-id>.jsonl`，打开确认内容覆盖全部阶段及追加追问。
4. 若同目录下有以该 session-id 命名的文件夹（含 `subagents/`、`tool-results/`），与主 JSONL
   一并打包为 ZIP，保留相对目录结构。
5. 将该文件（或其副本）放入本仓库的 `data/trajectories/`，或直接在标注工具页面里用"导入会话"
   按钮上传，供下一步标注工具读取。

`data/trajectories/` 已加入 `.gitignore`，放进去的文件不会被提交到仓库。

## 6. 用标注工具做人工复核

```bash
npm install   # 首次运行
npm start
# 打开工具地址（本地是 http://localhost:4173，公网部署时用面试官提供的地址）
```

工具支持多名候选人共用同一个部署，因此打开页面后先用**面试官分配给你的专属代码**登录——这个代码
就是你的凭证，不需要另外注册用户名或密码；登录后你只能看到、操作自己的数据，看不到其他候选人的。

登录后，左侧列表会自动扫描属于你的会话目录（按子文件夹分组显示，例如把某次尝试放进
`attempt1/` 分组就会在侧边栏归到"attempt1"下）和全体共享的 `samples/`（固定归到
"样本参考（只读）"分组），不需要手动放对路径再刷新——放进去或删掉都会在下次刷新时反映；也可以点
"导入会话"直接在页面里选文件上传，可选填一个分组名。侧边栏每个文件名旁边的"✎"能重命名文件本身
（同时会把已保存的标注迁移过去），只对自己导入/放入的文件生效，`samples/` 下的参考样本是只读的。

操作步骤：

1. 左侧列表点选待标注的会话文件（自己的作答放 `data/trajectories/`，练习用样本在
   "样本参考（只读）"分组下）。
2. 顶部一次性填写：作答人、使用工具、模型配置、最终代码链接（后两项在编码/推送完成后再补）。
3. 页面会按"真实用户输入"把轨迹切成一条条记录，按时间顺序逐条核对：用户需求、工具能力与约束、
   可见分析、工具调用与返回、代码修改、最终回复。
4. 对每条记录：
   - 确认/修正"阶段"下拉（初始实现／功能扩展／缺陷处理／代码理解）。
   - 填写"核查结论"：发现错误／未发现错误／暂不能判断。
   - 若发现错误，点击"添加问题点"，按第 7 节的编号与错误类型规则填写现象、判断理由、影响与
     恢复、改进建议；同一事实只记一个问题点，多个错误类型在同一问题点内分别说明理由。
   - 如有环境故障、用户误操作或无法确定的疑点，写入"异常与疑点"，不计入已确认的 Agent 错误。
5. 随时点"保存标注"，数据写入 `data/annotations/<会话文件名>.json`（同样已 gitignore）。
6. 全部记录标注完成后，点"导出 CSV"，列顺序与交付表字段一致，可直接整行粘贴进飞书的
   "笔试作答"表。

**重要**：轨迹通读、错误判断和标注文字必须由候选人本人完成，不得使用 AI 分析、摘要轨迹或辅助
打标签——本工具只负责展示和存储，判断部分仍需手动填写。

## 7. 错误类型速查

| 代码 | 含义 | 何时使用 |
| --- | --- | --- |
| E01 | 明确指令未遵循 | 违反指令已写明的范围/限制/禁止事项，优先于其他类型 |
| E02 | 需求理解偏差 | 理解错误、遗漏功能、边界未实现、只做表面 |
| E03 | 幻觉或失实陈述 | 编造不存在的 API/库/事实、谎报完成状态；当场被工具纠正且最终结论已改不算 |
| E04 | 工具使用错误 | 工具/参数/路径/依赖/命令用错；环境本身不可用记 ENV |
| E05 | 代码质量问题 | 有证据的功能/兼容/安全/维护缺陷 |
| E06 | 效率与冗余 | 重复调用、绕远路、过度设计；合理探索不算 |
| E07 | 沟通与解释问题 | 根因解释错、前后矛盾、答非所问、遗漏必要说明 |
| E08 | 偏离任务或越权操作 | 分心修非主线问题、擅加功能、未授权删文件、违禁启动服务 |
| ENV | 环境故障 | 非模型原因造成 |
| USER | 用户/题目问题 | 候选人误操作、主动转向、题目本身问题 |
| OTHER | 其他 | 确实无法归入以上任何一类时才用 |

## 8. 提交前检查清单

- [ ] 三个阶段（含追加追问）都在同一会话内完成，未中途重开会话。
- [ ] 原始 `.jsonl`（含子会话/工具输出）已从 CLI 会话目录导出，内容覆盖全部阶段。
- [ ] 每条实际发送给 Agent 的指令都在标注工具里生成了一条记录，且已填写核查结论。
- [ ] 发现错误的记录都按第 7 节规则填了编号、类型、现象、理由、影响与恢复、改进建议。
- [ ] 已用"导出 CSV"生成表格内容，逐行核对后誊入飞书"笔试作答"表。
- [ ] 最终代码已推送，`最终代码链接`指向与轨迹一致的 commit。
- [ ] `docs/`（题目原文）及任何 `.jsonl` 未被提交进仓库（已由 `.gitignore` 保证，推送前建议
      用 `git status` 复查一遍）。
- [ ] 在"笔试登记"表标记已完成。
