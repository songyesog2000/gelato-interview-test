{
  "name": "trajectory-annotation-tool",
  "version": "1.0.0",
  "description": "Web tool for loading Claude Code / Codex CLI session JSONL trajectories and annotating them per-record with issue codes.",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
